<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findbue">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindBue">
        <activity
            android:name=".RegistrarFamiliar"
            android:exported="true">
            <!-- Aqui las 5 lineas del intent-filter para ejecutar Registrar familiar -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".EditarPerfilPersona"
            android:exported="true" />
        <activity
            android:name=".RegUbiAdultoMayor"
            android:exported="true">

            <!-- Aqui las 5 lineas del intent-filter para ejecutar Registrar Adulto Mayor 2/2 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegistrarAdultoMayor"
            android:exported="true">

            <!-- Aqui las 5 lineas del intent-filter para ejecutar Registrar Adulto Mayor 1/2 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PanelPrincipalUsuario"
            android:exported="true">

            <!-- Aqui las 5 lineas del intent-filter para ejecutar a Panel Principal -->

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Aqui las 5 lineas del intent-filter para ejecutar a main -->

        </activity>
    </application>

</manifest>